% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Concert-binary.R
\name{Concert_R}
\alias{Concert_R}
\title{Variational Spike-and-Slab Transfer Regression (Concert)}
\usage{
Concert_R(
  X,
  y,
  n_vec,
  threshold = 1e-06,
  max_iter = 1000,
  tau = rep(1, length(n_vec) - 1),
  q_k = 0.2,
  eta = 1,
  q_0 = 0.05
)
}
\arguments{
\item{X}{A matrix of feature data.}

\item{y}{A vector of target variable values.}

\item{n_vec}{A vector indicating the size of each source dataset.}

\item{threshold}{The convergence threshold for the algorithm.}

\item{max_iter}{The maximum number of iterations.}

\item{tau}{A vector of hyperparameters for each dataset.}

\item{q_k}{The probability of a coefficient being non-zero for each dataset.}

\item{eta}{The learning rate for updating the coefficients.}

\item{q_0}{The prior probability of a coefficient being zero.}
}
\value{
A list containing the posterior estimates for the coefficients and other model outputs.
}
\description{
This function implements the Variational Spike-and-Slab Transfer Regression model.
}
\examples{
set.seed(42)
n1 <- 100; n2 <- 120; n3 <- 110
p <- 20
n_vec <- c(n1, n2, n3)
X1 <- matrix(rnorm(n1 * p), nrow = n1, ncol = p)
X2 <- matrix(rnorm(n2 * p), nrow = n2, ncol = p)
X3 <- matrix(rnorm(n3 * p), nrow = n3, ncol = p)
beta_true <- c(rep(2, 5), rep(0, 15))
y1 <- rbinom(n1, 1, plogis(X1 \%*\% beta_true + rnorm(n1, sd = 0.5)))
y2 <- rbinom(n2, 1, plogis(X2 \%*\% beta_true + rnorm(n2, sd = 0.5)))
y3 <- rbinom(n3, 1, plogis(X3 \%*\% beta_true + rnorm(n3, sd = 0.5)))
X <- rbind(X1, X2, X3)
y <- c(y1, y2, y3)
Concert_R(X, y, n_vec, threshold = 1e-6, max_iter = 100, tau = c(1, 1, 1),
         q_k = 0.2, eta = 1, q_0 = 0.05)
}
